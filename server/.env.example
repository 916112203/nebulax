# SmartPipe GIS 后端服务配置（复制为 .env 后按需修改）
PORT=8080
HOST=0.0.0.0

# 生产环境务必修改为随机长字符串
JWT_SECRET=smartpipe-demo-secret-change-me-in-production
JWT_EXPIRES=12h

# 数据库与种子数据路径（默认 server/data/ 下）
# DB_FILE=/data/smartpipe.db
# SEED_DIR=/data

# 遥测模拟参数
TELEMETRY_INTERVAL=3000
ANOMALY_CHANCE=0.04
